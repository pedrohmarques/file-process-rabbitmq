import { Routes } from '@angular/router';
import { FileProcess } from './view/file-process/file-process';

export const routes: Routes = [
    {
        path: '',
        component: FileProcess
    }
];
